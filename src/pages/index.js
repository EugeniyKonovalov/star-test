import Layout from "components/layout/layout";
import Profile from "components/profile/profile";

const Home = () => {
  return (
    <Layout>
      <Profile />
    </Layout>
  );
};

export default Home;
